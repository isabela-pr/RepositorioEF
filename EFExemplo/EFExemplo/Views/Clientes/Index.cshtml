@model IEnumerable<EFExemplo.Models.Cliente>

@{
    ViewData["Title"] = "Cadastro de Clientes";
}

<h1>Cadastro de Clientes</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Adicionar</a>
</p>
<table class="table table-light table-striped">
    <thead>
        <tr class="">
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascimento)
            </th>
            <th></th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataNascimento)
                </td>
                <td class="text-end">
                    <a asp-action="Edit" class="btn btn-outline-warning" asp-route-id="@item.ClienteId">
                        <svg bootstrap-icon="PencilFill" width="16" height="16" aria-label="Alarms"></svg>
                    </a>
                    <a asp-action="Details" class="btn btn-outline-info" asp-route-id="@item.ClienteId">
                        <svg bootstrap-icon="CardList" width="16" height="16" aria-label="Alarms"></svg>

                    </a>
                    <a asp-action="Delete" class="btn btn-outline-danger" asp-route-id="@item.ClienteId">
                        <svg bootstrap-icon="Trash" width="16" height="16" aria-label="Alarms"></svg>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
